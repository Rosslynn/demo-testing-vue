describe('anime', () => {
  describe('mutations', () => {
    describe('SET_ANIMES', () => {
      it('should add the list of animes to the state', () => {});
    });

    describe('SET_ANIME', () => {
      it('should add the anime from which you are getting more information about it to the state', () => {});
    });
  });

  describe('actions', () => {
    describe('getAnimes', () => {
      it('should get the list of anime and save them in the state', () => {});
    });

    describe('getAnime', () => {
      it('should obtain information on a specific anime and save it in the state', () => {});
    });
  });
});
